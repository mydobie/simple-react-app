/* eslint-disable react/react-in-jsx-scope */
import { mount, shallow } from 'enzyme';
import { axe } from 'jest-axe';
import Alert from '../../components/Alert';

describe('Feature Flag tests', () => {
  test.todo('Is accessible');
});
